<?xml version = "1.0" encoding = "utf-8" ?>
<component name="MainMenu" extends="Group">
    <script type="text/brightscript" uri="MainMenu.bs" />

    <interface>
        <field id="buttonSelected" type="string" alwaysNotify="true" />
    </interface>
    <children>
        <LayoutGroup id="mainContainer"
            layoutDirection="horiz"
            itemSpacings="[9]">
            <Button id="homeBtn" />
            <Button id="detailsbtn" />
            <Button id="newsBtn" />
            <Button id="profilebtn" />
        </LayoutGroup>

    </children>
    
</component>